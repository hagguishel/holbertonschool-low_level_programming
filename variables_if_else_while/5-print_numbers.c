#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/* more headers goes there */
/* betty style doc for function main goes there */
int main(void)
{
int digit;
for (digit = 0; digit <= 9; digit++)
{
printf("%d", digit);
}
return (0);
}
