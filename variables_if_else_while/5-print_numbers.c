#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: 0 if the program executes successfully
 */
int main(void)
{
	int i = 0;

	for (; i < 10; i++)
	{
		printf("%d", i);
	}
	printf("\n");
	return (0);
}
